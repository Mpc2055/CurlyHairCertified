/**
 * @deprecated This file is maintained for backwards compatibility
 * Use the new API client and query setup instead:
 * - API client: import { api } from "@/lib/api-client"
 * - Query client: import { queryClient } from "@/lib/query"
 * - Query keys: import { queryKeys } from "@/lib/query-keys"
 */
export { apiRequest } from "./api";
export { queryClient } from "./query";
